A small employee salary prediction app made using streamlit

Tried different ML models on employee salary dataset
XGBoost was selected as best model from pipeline and used it in the app for prediction
